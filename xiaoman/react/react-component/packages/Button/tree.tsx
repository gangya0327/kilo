import React, { useState } from 'react'
import { TreeNode, TreeProps } from './type'
import './style.css'

const Button: React.FC<TreeProps> = ({ data, onChange }) => {
  return <div>Tree</div>
}

export default Button
